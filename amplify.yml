version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build required for simple HTML/CSS/JS project"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []